import { PagePlaceholder } from "@/components/shared/page-placeholder";

export default function EventsPage() {
  return (
    <PagePlaceholder
      description="Danh sách sự kiện sẽ được kết nối với backend trong bước tiếp theo."
      eyebrow="Sự kiện"
      title="Khám phá sự kiện"
    />
  );
}
